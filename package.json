{
  "name": "react-3",
  "version": "0.1.0",
  "scripts": {
    "clear": "rimraf dist .parcel-cache .cache",
    "dev": "npm run clear && parcel src/index.html",
    "build": "npm run clear && parcel build src/index.html --public-url . --no-source-maps --no-minify"
  },
  "dependencies": {
    "react": "^16.14.0",
    "react-dom": "^16.14.0"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.1",
    "rimraf": "^3.0.2"
  }
}
